<!-- 


jQuery의 기본 문법 

$(selector).action()

1. $ 문자는 jQuery를 선언하거나 접근할 때 사용한다 ($문자는 jQuery의 별칭(alias)이다)

2. select는 CSS의 선택자를 의미한다
   일반적으로 HTML의 특정 DOM을 참조하기 위하여 사용한다

3. action()은 HTML에서 특정 이벤트가 발생할 때 실행되는 함수이다


예)
class=".blue"인 태그를 hide시킨다
$(".blue").hide();

<p>태그 내의 내용을 반환한다
$("p").text();

모든 <li> 태그 중에서 짝수 위치의 <li> 태그의 내용을 빨간색으로 변경한다
$("li:even").css("color","red");


-->

